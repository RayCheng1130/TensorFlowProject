# TensorFlowProject
Used for TensorFlow learning and couese project for CS559.

> for the html file I tried to train the model to steer the call moving inside a box without hitting the coundaries. The training data is generated by hard-written rule.